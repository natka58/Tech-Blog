<div class="comments">
  
    {{#each comments}}
        <section class="comment">
            <div class="meta">
                {{User.username}} on {{format_date created_at}}
            </div>
            <div class="text">
                {{comment_text}}
            </div>
        </section>
    {{/each}}
</div>
