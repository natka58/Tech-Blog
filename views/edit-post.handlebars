<article>
    <a href="/dashboard" class="is-pulled-right"> &larr; Return to dashboard</a>
    <h2 class="title mt-5">Edit post</h2><hr>
    <form class="edit-post-form">

       <div>
            <label for="post-title">Title:</label>
            <input class="input" name="post-title" type="text" value="{{post.title}}" />
        </div>
         <div>
            <label for="post-content">Post content:</label>
            <input class="textarea" name="post-content" type="text" value="{{post.post_content}}" />
        </div>
         <div class="is-pulled-right">
            you created post on {{format_date post.created_at}}
            |
            <a href="/post/{{post.id}}">{{post.comments.length}} {{format_plural "comment" post.comments.length}}</a>
        </div>
        <button type="submit" class="button is-success">Save</button>
        <button type="button" class="button is-danger delete-post-btn">Delete</button>
    </form>
</article>
{{> comments post.comments}}
{{!-- <div class="post card">
  <div class="card-header">
    <h2>Edit Post</h2>
  </div>
  <form id="edit-post-form" class="card-body">
    <input type="hidden" name="post-title" value="{{post.id}}" />
    
    <label class="form-label" for="post-title">Title</label>
    <input type="text" name="post-title" value="{{post.title}}" class="form-input" />

   <label class="form-label" for="post-body">Content</label>
   <textarea name="post-body" class="form-input">{{post.body}}</textarea>

   <button type="submit" class="btn">Update</button>

   <button type="button" id="delete-btn" class="btn">Delete</button>
  </form>
</div> --}}


{{!-- <div class="row">
  <div class="col-11 mx-auto my-2 mb-0">
    <div class="card border border-dark rounded">
      <form class="edit-post-form">
        <div class="card-header text-white bg-dark">
          <input name="post-title" type="text" value="{{post.title}}" placeholder="{{post.title}}"/>
          <p class="subtext">Posted by you on {{format_date post.created_at}}</p>
        </div>
        <div class="card-body">
          <p class="card-text">{{post.post_content}}</p>
          <a href="/post/{{post.id}}">{{post.comments.length}} {{format_plural "comment" post.comments.length}}</a>
          <button type="submit" class="btn btn-outline-success">Save post</button>
          <button type="button" class="btn btn-outline-danger delete-post-btn">Delete post</button>
        </div>
      </form>
    </div>
  </div>
  <div class="col-11 mx-auto my-2">
   <form class="new-post-form comment-form input-group px-0">
      <textarea class="form-control" name="comment-body" rows="3"></textarea>
      <button type="submit" class="btn btn-outline-success">Add comment</button>
    </form>
  </div>
  <div class="col-11 mx-auto my-2">
    <div class="list-group">
      {{> comments post.comments }}
    </div>
  </div>
</div> --}}

<script src="/javascript/edit-post.js"></script>
<script src="/javascript/delete-post.js"></script>
<script src="/javascript/comment.js"></script>